package bubble.test.ex11_어댑터패턴과인터페이스Default;

// Enum 클래스 : 모든 자바 열거체의 공통된 조상 클래스
// 열거형 : 서로 연관된 상수들의 집합

public enum PlayerWay {
	// 3-1 물방울의 방향은 2가지(오른쪽, 왼쪽)라서 플레이어의 방향도 두가지만 알면 됨
	LEFT, RIGHT;	// 3-2 방향 설정 후 다시 Player.jave 로 이동
}
